<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to 'WiFiCiTi', root_path %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>


  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <li class="divider"></li>
      <li><%= link_to 'Find Wifi', find_me_users_path %></li>
      <li class="divider"></li>
      <% if current_user && current_user.admin %>
        <li><%= link_to 'Hotspot Directory', hotspots_path %></li>
        <li class="divider"></li>
        <li><%= link_to 'View Users', users_path %></li>
        <li class="divider"></li>
      <% else %>
        <% if current_user %>
          <li><%= link_to 'My Account', current_user %></li>
          <li class="divider"></li>
        <% end %>
      <% end %>
    </ul>

    <ul class='right'>
      <li class="divider"></li>
      <% if current_user %>
        <li><%= link_to "Sign Out", logout_path, method: :delete %>
      <% else %>
        <li><%= link_to 'Register', new_user_path %></li>
        <li><%= link_to 'Sign in', login_path %></li>
      <% end %>
    </ul>
    
  </section>
</nav>
<% if current_user && current_user.admin? %>
    logged in as admin
    <% else %>
    not logged in as admin
    <% end %>
